# Core Dependencies
numpy>=1.24.0
scipy>=1.10.0
python-dotenv>=1.0.0
httpx>=0.24.0

# Audio Processing
# Note: pyaudio removed - sounddevice is a better cross-platform alternative
sounddevice>=0.4.6
soundfile>=0.12.1
librosa>=0.10.0
# webrtcvad requires compilation - using silero-vad instead

# Speech Recognition and Transcription
faster-whisper>=1.0.0,<1.3.0
# RealtimeSTT removed - causes dependency conflicts, using faster-whisper directly
silero-vad>=4.0.0

# Speaker Diarization
pyannote.audio>=3.1.0
# speechbrain removed - ruamel.yaml.clibz requires C++ compiler
# resemblyzer removed - webrtcvad requires C++ compiler
# Using pyannote.audio which provides speaker embedding capabilities

# Wake Word Detection
openwakeword>=0.5.1
pvporcupine>=2.2.0

# LLM and RAG
openai>=1.6.0
google-generativeai>=0.3.0
langchain>=0.1.0
langchain-community>=0.0.10
chromadb>=0.4.18
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4

# TTS
cartesia>=0.1.0
pyttsx3>=2.90

# Meeting Integration
selenium>=4.15.0
playwright>=1.40.0
pyautogui>=0.9.54

# Utilities
psutil>=5.9.0
colorlog>=6.8.0
tqdm>=4.66.0
pydantic>=2.5.0
pyyaml>=6.0.1

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Optional GPU Support
# torch>=2.1.0  # Uncomment for GPU support
# torchaudio>=2.1.0  # Uncomment for GPU support
